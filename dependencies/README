OS X:

You must install libgmp3-dev on OS X
use MacPorts or Fink to get a port of this library


On OS X you may get a compiler error that malloc.h is not found. Solution:
sudo ln -s /usr/include/malloc/malloc.h /usr/include/malloc.h


